apiVersion: v1
kind: Service
metadata:
  name: {{ include "alertmanager-graph-bridge.fullname" . }}
  labels:
    {{- include "alertmanager-graph-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "alertmanager-graph-bridge.selectorLabels" . | nindent 4 }}
